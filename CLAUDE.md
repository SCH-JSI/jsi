# JSI · S&C Holdings

JSI is an S&C Holdings project: a public shelf of standalone HTML projects hosted with GitHub Pages.

When the user asks to publish, post, ship, upload, add, or put an HTML page or project in JSI, use the `publish-jsi` skill. Never push a new project straight to `main`: give the user a draft preview first and merge only after explicit approval.
